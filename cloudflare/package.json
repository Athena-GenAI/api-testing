{
  "name": "smart-money-api",
  "version": "1.0.0",
  "description": "Smart Money Position Tracker API using Cloudflare Workers",
  "main": "workers/api/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:prod": "wrangler deploy --env production",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0",
    "jest": "^29.0.0"
  }
}
